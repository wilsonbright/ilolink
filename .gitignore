# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Env / secrets
.env
.env.*
!.env.example

# Env / secrets (Cloudflare local dev vars)
.dev.vars
.dev.vars.*

# Deps / build
node_modules/
dist/
build/

# Next.js
.next/
next-env.d.ts

# OpenNext + Cloudflare
.open-next/
.wrangler/
cloudflare-env.d.ts

# Cloudflare deploy creds (never commit)
.cf.env

# TS incremental build cache
*.tsbuildinfo

# Test artifacts
.playwright-mcp/
*.png

# Public build vars only (NEXT_PUBLIC_*) — never real secrets
!.env.production
